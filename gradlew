#!/bin/sh
##############################################################################
## Gradle start up script for UN*X
##############################################################################
APP_HOME=$(dirname "$0")
APP_HOME=$(cd "$APP_HOME" && pwd)
exec "$APP_HOME/gradle/wrapper/gradle-wrapper" "$@"
